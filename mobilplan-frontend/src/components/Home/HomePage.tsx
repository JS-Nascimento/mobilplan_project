// Em um arquivo separado, por exemplo, HomePage.jsx
import React from 'react';
import { Typography } from '@mui/material';

const HomePage = () => (
    <Typography variant="h4"></Typography>
);

export default HomePage;
